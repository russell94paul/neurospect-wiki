# Neurospect Wiki Operations Log

Append-only chronological record of all wiki operations (ingest, query, lint).

| Timestamp | Operation | Summary |
| 2026-07-24 | build | **Learning Platform UI — Phase 5f (journal + expectancy, in `neurospect-learn`).** Built the empirical proof-of-edge loop over the existing 5c `journal_entries` table — **no migration**. **Backend:** `schemas/{journal,analytics}.py`; the **pure** `services/expectancy.py` (win/loss classified by `r_multiple` SIGN not the `outcome` enum → the identity `expectancy == mean(r_multiple)` holds exactly, asserted both ways; break-even `1/(1+avg rr)`; `SAMPLE_TARGET=50` is a **reference**, explicitly NOT the 5g gate verdict); `routers/journal.py` (CRUD + `mode`/`entry_model`/`instrument`/date filters + soft-delete, auth-gated + user-scoped) + `routers/analytics.py` (`/expectancy` by model×mode, `/summary` per mode, `/r-distribution`), mounted in main.py. **Frontend:** `recharts` re-added; Journal+Analytics types; `lib/journal.ts` (writes invalidate journal **and** analytics) + `lib/analytics.ts`; `components/journal/{journal-form [tabbed RHF+Zod, backtest\|live toggle],journal-card,journal-filters}` + `components/analytics/{chart-common,expectancy-chart,backtest-vs-live-chart,r-distribution-chart}` (2-hue palette blue-backtest/orange-live **validated via the /dataviz script — all six checks pass both light+dark**); pages `{journal,journal-entry,expectancy}` replaced the 4 stubs. **Verified (evidence):** backend **33 tests** (16 prior + 9 pure-expectancy hand-fixture + 8 journal API incl. soft-delete/isolation/enum-422/no-token-403/expectancy-VIEW); `tsc -b`+`vite build` clean; **Playwright 20/20** (17 prior + 3 journal); live claude-in-chrome walkthrough (create backtest entry → list → expectancy dashboard) with **NO console errors**, rendered numbers cross-checked by hand (Backtest +0.64R/win55%, Live −0.33R/win33%). Reconciled `learning-platform.md` (§Route taxonomy + §journal data model §3 + §Component/API surface → as-built + §5f as-built + split ✅); did NOT touch trade-schema/phase3-frontend-structure. Tracker 5f ✅ + session log + 5g boot prompt written (⏭ ACTIVE). Isolation clean; git untouched (Paul commits). Next: Phase 5g — gate/readiness (completes the Phase 5 arc). |
| 2026-07-18 | design | **Learning Platform — Phase 5a (new-app design session).** Designed `neurospect-learn`, a new standalone app delivering the mastery-layer content + tracking progress across learning / backtesting / live axes, graded on the existing ladder/confidence/Readiness-to-Live Gate. Surveyed the content spine (`mastery/README`, `unified/{README,learning-path,tracker}`) + canonical architecture docs (`phase3-frontend-structure`, `trade-schema`, `phase2-project-structure`, `phase4-coach-frontend`, `monorepo-migration`); one Explore agent inventoried `neurospect-app/src` for lift-vs-leave-behind. Put three product forks to Paul via AskUserQuestion; he redirected all three from my recommendations: **(1) separate self-contained backend** (own FastAPI+Postgres+Discord auth+expectancy engine, not sharing `neurospect-api`); **(2) a brand-new model-aligned journal** whose fields trace to the taught unified/entry-model decision flow (`mode: backtest\|live` powers both axes) — not the generic `trades` schema; **(3) Content API** served by the new backend via an ingest job (wiki stays canonical, authoring stays in Obsidian). Wrote ONE canonical doc `concepts/architecture/learning-platform.md` (new-app shape + repo siting under monorepo-migration; lifted frontend stack [React19/Vite8/Router7/TanStackQuery5/RHF7+Zod4/ky2/shadcn/Recharts3/Tailwind4] + mirrored backend layout; U0–U6 route taxonomy; content API + ingest; three-part data model = `concepts`+`concept_progress` / model-aligned `journal_entries` / computed gate; component + API surface; 5b–5g implementation split). Named `learning-platform.md` (not the boot prompt's `-frontend`) because the design spans a full backend. **Flagged (Rule #6):** shipped `neurospect-app` versions (Vite 8 · TS 6 · ky 2 · Zod 4 · Recharts 3 · Tailwind 4 · Router 7.14) diverge from `phase3-frontend-structure.md` (Vite 6 · ky 1) — code is ground truth; flagged for Paul, NOT edited (that doc is the journal-analytics lane's). Added 5b–5g phases + Phase 5a session log to `learning-platform-ui.md`; additive cross-links from `mastery/README` + `unified/README` + `phase3-frontend-structure`; updated `index.md` (Architecture line + `updated`/`last_design`). Isolation clean (no ALDC refs); no-drift honored (doc LINKS content pages, never restates); design-only (no app code). Next: Phase 5b — scaffold `neurospect-learn`. |
| 2026-07-17 | research | **Mastery layer — Phase 3 (course-content deep research: unified model + frontier ICT).** Two thrusts. **Thrust A (unified model, direct synthesis of the existing corpus):** new `concepts/mastery/unified/` namespace filling the reserved slot. `README.md` = the Unified Playbook — one principled 5-layer sequence (psychology → structural primitives → nested-SMT confirmation → execution → risk) with a one-glance decision flow; `divergence-rulings.md` = 8 rulings (R1–R8) reconciling every flagged AXL↔Aura divergence (SMT role → COEXIST-LAYERED; swing FVG-filter vs SMT-filter → COEXIST; quadrants vs discount/EQ/premium → COEXIST; order-block vocab → SUPERSET; **risk fixed-$ vs %/R → SUPERSEDE %/R, a previously-UNFLAGGED divergence surfaced per Rule #6**; Time Sum → DROP; 6S → ADOPT-additive; sequencing → psychology-first) + convergences. Two new EMERGING confluences fell out: the **nested SMT stack** (Sequential SMT anchors structure, same-TF triad SMT triggers entry) and the **double-qualified swing** (FVG+SMT = top-tier anchor). **Thrust B (frontier content, focused web deep-research via 3 parallel Sonnet research agents → adversarial verify → tier+label):** new `concepts/advanced/` namespace: `README.md` (Tier-1/2/3 rubric + ESTABLISHED/EMERGING/SPECULATIVE labels + the cross-cutting WHERE/WHEN/DIRECTION/CONFIRM confluence stack + the unfalsifiability critique), `ict-macros-and-silver-bullet.md` (Silver Bullet ESTABLISHED/Tier-1; macros community-reconstructed; Judas-swing micro-structure; killzone-boundary contradiction flagged), `ipda-data-ranges.md` (20/40/60 ESTABLISHED; "liquidity matrix" debunked → SPECULATIVE, real term is PD Array Matrix; IRL/ERL; LRLR/HRLR community-shorthand), `quarterly-theory.md` (EMERGING — Trader Daye, NOT ICT; 90-min cycles; True-Day-Open 3-referent disambiguation), `cbdr-and-sd-projections.md` (CBDR/flout ESTABLISHED; SD=arithmetic-range-multiple ≠ Fibonacci ≠ statistical std-dev disambiguation; index-futures pip→point calibration gap flagged). Every claim cited (source URL / transcript path); contradictions surfaced not smoothed; marketing win-rates (71%) explicitly rejected. Honesty note carried: web pass captured almost no Tier-1 verbatim (YouTube/X un-fetchable) → most claims corroborated-Tier-2. **Out of scope (per tracker):** backtesting, app integration, graded roadmap (next session). Updated `mastery/README.md` (Unified row Deferred→Built + slot note), `index.md` (Unified + Advanced sections), tracker Phase 3 ✅. No-drift honored (LINK, never restate canonical `ict-*`/`aura-*`/entry-model pages); isolation clean (no ALDC refs). |
| 2026-07-17 | plan | Created workstream tracker `processes/distributed-workflow/active/mastery-layer.md` (goal, lane, decisions, phases 1✅/2⏳/3⛔-deferred, Phase 1 session log, and the Phase 2 ICT-course-track boot prompt with the Explore-agent inventory findings baked in). Cataloged in `index.md` Distributed Workflow section. |
| 2026-07-17 | build | **Mastery layer — Phase 1 (mastery system + Aura track).** New `concepts/mastery/` namespace: the learn-to-execute layer for taking a model from "understood" to "live-ready" with evidence-based readiness tracking. Wrote `mastery/README.md` (hub: 4-stage ladder Learned→Can-mark→Backtested→Live-ready, 1–5 confidence scale, rep counters, and the evidence-gated Readiness-to-Live Gate; two-track model with a deferred unified-model slot). Built the **Aura track** (5 pages): `aura/rules.md` (54-rule executable rulebook distilled + cited to aura-NN across all 14 aura concept pages; divergences/flags preserved, not hardened), `aura/checklist.md` (pre-market→framing→entry→management→exit→review sheet with per-rule [R##] refs + a copyable Per-Trade Card mirroring trade-schema fields; fitness-checked against aura-24), `aura/learning-path.md` (Stage 0 psychology → Stage 6 live, each stage = concepts + drills + gate), `aura/exercises.md` (drill library D0-a…D6-c with ✋ hand-marking + 🛠 tool-assisted variants, rep targets, and the 13 worked-review replay dates), `aura/tracker.md` (living per-concept ladder/confidence/reps + backtest-expectancy table + readiness gate). No-drift rule honored: mastery pages LINK to concept/entry-model pages as source-of-truth, never restate. Isolation rule respected (no ALDC refs). Updated `index.md` (new Mastery section + `updated`). Decisions from Paul: build both tracks then a later unified-model deep-research session; full tooling available so drills are tool-assisted + hand-marking for eye-training. No blocking data gaps (no annotated chart images exist → replay on exact historical dates + own annotated journal is the mitigation). Next: Phase 2 — ICT-course track (rules aggregator + exercises index/gap-fill + tracker), seeding tape-reading drills from the 11 stream + 2 YouTube transcripts. |
| 2026-07-17 | build | **Mastery layer — Phase 2 (ICT-course / MrWitness-AXL track).** Built 3 pages in `concepts/mastery/ict-course/`, mirroring the Aura track's structure/tone. Scoped via 3 parallel Explore agents (Sonnet): course-lesson rules+homework inventory, entry-model YAML extraction, business-logic checklist loci — findings fed authoring directly. **`rules.md`** = 41-rule aggregator (A foundations · B price delivery · C session/bias · D market structure · E order flow/SMT · F execution discipline) normalizing rules scattered across 16 lessons under inconsistent headings (`## Rules` / `Key Rules` / `Closing Basis Rules` / `The Rule` / `Daily Checklist` / `Full Sequence`, absent in several); every rule LINKS to its lesson + entry-model YAML (never forks the AI-coach-consumed `checklist:` arrays); reconciles the two extra loci (`ict-entry-models`#Entry Checklist → subsumed by entry-models/README §Minimum Confluence; `ict-live-commentary` pre-market routine + AXL discipline → §F), flags left open not silently resolved. **`exercises.md`** = drills index + gap-fill, stages mapped 1:1 onto course Modules 1–5 (not a new system) with ✋ hand-mark + 🛠 tool variants: indexed existing homework (M1.1/M1.3 + all M3, M4.1-2, M5) incl. the reusable `## Readiness Check` checkbox primitive; **gap-filled** the lessons with no homework (M2.1/2.3/2.4, M4.3, M4.4); added 13 tape-reading drills (T-01…T-13) from the 11 stream + 2 YouTube transcripts tagged by news context (NFP-Fri / FOMC-Wed / JOLTS-Tue / Fed-testifies / ISM-PMI / no-news-Mon, etc.). **Accuracy correction to the boot prompt:** the 11 stream transcripts have `video_date: unknown` → cannot replay exact dates, so their drills are "study the read → replay a *comparable* news-context session"; only the 2 YouTube examples carry precise dates (2026-03-04, 2026-03-07) for exact-date replay. **`tracker.md`** = per-lesson (16) + per-entry-model (7) ladder/confidence/reps, module readiness gates, backtest-expectancy table, and the Readiness-to-Live Gate with AXL daily-loss-limit specifics substituted. Did NOT create ict-course/learning-path.md or checklist.md (reuse `course/README` + entry-model YAML, per Decisions). Additive cross-links up into mastery from `course/README` + `entry-models/README`; updated `mastery/README` (ICT row = Built, See Also). Updated `index.md` (Mastery section expanded, tracker line = Phases 1–2 done), tracker Phase 2 ✅. Isolation clean (no ALDC refs); no-drift honored (links, not restates). Next: Phase 3 (unified-model deep-research) — deferred until both tracks backtested. |
| 2026-07-16 | ingest | Aura ingest **Phase 3 (reconciliation)**. Enriched the verified MrWitness-AXL canon with **attributed** Aura (dOoMeR) content — additive only, contradictions flagged (CLAUDE #6), two-way cross-refs, never silently merged. Fanned out 4 parallel Sonnet subagents on non-overlapping write lanes; Opus main session did the decided-spec pieces + reviewed every merge via git diff (all deletions confirmed frontmatter-only, zero body content removed). Merges: **`ict-smt.md`** ← Sequential SMT (time-cycle nesting), Sequential Skip, Pearson math-first triad selection (5 triads), Aura Asset (6S, wholly-new dollar-proxy material) + a convergence callout (both mentors independently land on YM's diverging role) + a primary-vs-confluence divergence callout. **`ict-liquidity` / `ict-market-structure` / `ict-order-flow`** ← Aura ranges (discount/EQ/premium), gaps + "what lies within", 3-candle swing pivot with SMT-qualification (vs MrWitness gap-qualification), HTF→LTF if-then cascade; two required divergence callouts flagged (quadrants; order-block vocabulary). **entry-models** `README` + `smt-confirmation-entry` ← Aura Sequential SMT entry model (iFVG-first in range discount, Sequential Skip fallback, cross-asset variant). **4 roadmap ideas** ← attributed Aura evidence (reduce-journaling-friction, trader-psychology-profiler, overtrading-risk-limits, mistake-driven-action-items). Main-session pieces: **`trade-schema.md`** — specced the decided new lightweight **`missed_trades`** table (separate from `trades`; miss_type almost_took/hesitated/canceled, hesitation_tags, planned entry/stop/target, hypothetical_outcome + hypothetical_r, screenshot child table; 2 ENUMs, 2 tables, 4 indexes, 3 analytics endpoints incl. opportunity-cost) + noted in `journal-analytics.md`; fixed a pre-existing duplicate `sources:` key. **`doomer.md`** deepened from the 14 synthesized pages (teaching approach, framework table, named influences, honesty markers, reconciliation status). Patched `concepts/aura/ranges.md`+`gaps.md` with "cracking correlation" cross-refs (real term the Phase-2 structure agent dropped as garbled) and added reverse aura→ict links across 7 aura pages. Updated `index.md` (Aura section = reconciled, last_ingest, tracker line), `log.md`, tracker (Phase 3 ✅). Isolation rule respected — no ALDC references introduced. Next: Phase 4 close-out (this entry) + lint. |
| 2026-07-16 | ingest | Aura ingest Phase 2 (standalone synthesis) + caption-accuracy verification. Fanned out 7 parallel Sonnet subagents → 14 pages in `concepts/aura/` (psychology-foundations, mind-and-emotional-control, discipline-systems, journaling-system, swing-points, ranges, gaps, triads-asset-selection, sequential-smt, aura-asset, time-sum, htf-ltf-application, risk-management, trade-reviews) + upgraded README to a framework overview. Kept fully isolated from `ict-*` pages (reconciliation = Phase 3). Lane-compliance verified via git. Key decoded findings: **Aura Asset = Swiss Franc futures (6S)** universal 4th triad leg (chosen over DXY = not a synced futures contract; origin story flagged by dOoMeR as unverified personal theory); **Sequential SMT** = SMT nested across ≥2 time cycles; **Sequential Skip** = fallback confirmation; **Triad** = Pearson-correlation-selected asset sets (5 triads); **Time Sum** (369) de-emphasized by dOoMeR himself; psychology/risk built on Tom Dante ("Blueprint"), Mark Douglas, Atomic Habits, Huberman. Accuracy A/B: re-transcribed 2 videos with local Whisper medium (CPU) and diffed vs captions — v6=91.2%, v15(jargon)=95.1% word match, ~99% content; divergences are filler/formatting + a jargon wash (Whisper won proper names Springmeier/Bloodlines/"franc"; caption won "triad"). Synthesis layer self-corrected every ASR slip from context. **Captions validated — no re-transcription needed.** Updated `index.md` (14 pages), tracker (Phase 2 ✅, Phase 3 boot prompt + per-target reconciliation plan), `log.md`. Next: Phase 3 reconciliation into canonical `ict-*` pages (attributed, contradictions flagged). |
| 2026-07-16 | ingest | Aura playlist (dOoMeR, 2nd ICT/SMC mentor) Phase 0+1. Transcribed 30/30 YouTube videos (~93k words) via yt-dlp auto-captions + custom rollup-dedup cleaner (`clean_vtt`: last-line-of-cue rule) → `sources/neurospect/aura/aura-01..30-*.md` (immutable). Torch is CPU-only so Whisper (~12-18h) avoided; captions clean across all 30, proprietary jargon (Sequential SMT, Aura Asset, Time Sum, Sequential Skip, Triads) transcribed reliably (195 hits verified). Resolved mentor = dOoMeR (`@doomer312`); psychology-first curriculum (vids 1-5) is novel vs MrWitness-AXL corpus. Created `entities/people/doomer.md`, `concepts/aura/README.md` (scaffold), tracker `processes/distributed-workflow/active/aura-ingest.md`. Updated `index.md` (People, new Aura concept section, tracker, Sources). Approach: standalone-first then reconcile (Phase 3) to protect verified notes. Next: Phase 2 standalone synthesis into `concepts/aura/` via parallel subagents. One playlist video hidden/unavailable (not transcribed). |
| 2026-05-02 | plan | Created monorepo-migration tracker. Scope: merge `neurospect-wiki` + `neurospect-api` + `neurospect-app` into a single `neurospect` monorepo with `wiki/` `api/` `app/` sibling dirs. Phase 0 = design/approval (git history strategy, CLAUDE.md composition, deploy cutover sequence, repo name, coordination, old-repo handling). Spec not yet approved. Open questions logged for Paul. |
| 2026-04-26 | ops | Security cleanup complete. Rotated TRADINGVIEW_WEBHOOK_SECRET and per-user webhook token. Enabled TRADINGVIEW_IP_ALLOWLIST (4 TradingView IPs). Webhooks verified still working post-redeploy. Updated deployment tracker and project entity. |
| 2026-04-26 | build | Journaling UX Phase 1 code complete. Pine script synced v5→v6 (`public/neurospect-coach.pine`). `trade-form.tsx` rewritten with shadcn Tabs (Pre-Trade/Entry/Post-Trade), default date=today/instrument=NQ, "Save Trade" label on create. `pre-trade-fields.tsx` rewritten: 6 prominent fields + Advanced collapsible (6 optional fields, collapsed by default). `tsc -b` clean. R2 env vars still need manual Render setup before screenshots work. Push to main triggers Cloudflare auto-deploy. Updated `entities/projects/neurospect.md` and tracker session log. |
| 2026-04-26 | plan | Journaling UX workstream created. Paul feedback: form has too many fields, wants tab-based pre-trade/entry/post-trade. Designed tab layout, field reduction (12 pre-trade fields → 6 prominent + Advanced accordion), defaults (today/NQ). Created tracker at `processes/distributed-workflow/active/journaling-ux.md`. Chrome extension scoped as separate session. Updated reduce-journaling-friction idea page with user feedback and detailed Phase 1 spec. |
| 2026-04-26 | deploy | Phase 4 complete. TradingView Pine Script → webhook → Claude → coach panel verified end-to-end in prod. Key pitfalls: AI_COACH_PROMPT_DIR pointed at local wiki path on Render (deleted env var), CLAUDE_MAX_TOKENS=2048 too small (set 8192), barstate.isrealtime in Pine bgcolor triggers TradingView repaint warning that silently blocks all alert() delivery (fix: remove from plot variables), Pine v5→v6 required. Env vars added: CLAUDE_MAX_TOKENS=8192, CLAUDE_TIMEOUT_SECONDS=60. Todo: rotate exposed tokens, wire R2, enable IP allowlist. |
| 2026-04-25 | deploy | Phases 2+3 complete, Phase 4 partially verified. Render web service live (Python 3.13, Alembic in startCommand), Cloudflare Pages live, Discord OAuth working, trade journal saving. Key pitfalls: postgresql:// vs postgres:// shim, FastAPI 0.115 204-route assertion on Python 3.14, preDeployCommand ignored for manual services, DISCORD_CLIENT_ID/SECRET missing from initial env import. R2 screenshots and TradingView webhook deferred. |
| 2026-04-25 | plan | Product roadmap section created at `concepts/roadmap/`. Wrote `concepts/roadmap/README.md` (canonical roadmap with horizons Now / Next / Later / Strategic / Research / Compliance-Sensitive, status lifecycle convention, prioritization rationale) and `concepts/roadmap/ideas/README.md` (backlog index). Created 15 idea stubs under `concepts/roadmap/ideas/`: reduce-journaling-friction, tradovate-integration, chrome-screenshot-extension, mistake-driven-action-items, multi-account-portfolio-analytics, overtrading-risk-limits, trader-psychology-profiler, neurospect-voice, in-app-course-section, ui-design-exploration, platform-consolidation, vertical-ai-platform, agent-as-a-service, ai-live-audio-commentary, neuro-quant-fund. Each stub has `status: backlog` + `horizon:` frontmatter. Added top-level Roadmap section to `index.md`. Added `> Superseded for forward planning` callouts above the v1 *Phased Roadmap* and *Moonshot Ideas* sections in `entities/projects/neurospect.md` (content preserved). Added roadmap link to See Also in deployment, ai-coach, journal-analytics, course-and-kb trackers. Naming placeholders flagged: NeuroSpect Voice, Neuro-Quant, Neuro-Fund. Compliance-sensitive items (live audio commentary, Neuro-Fund) carry explicit constraints in their pages — do not use in marketing copy without legal review. |
| 2026-04-24 | deploy | Phase 1 backend deployment prep complete. `neurospect-api`: prompt files bundled to `app/coach/prompts/`, `config.py` updated (prompt dir default + `async_database_url`/`sync_database_url` shim properties + `database_url_sync` made optional), `database.py` + `alembic/env.py` updated to use shim properties, `gunicorn ^23.0` added + `poetry.lock` generated + `requirements.txt` exported (51 lines, pinned), `render.yaml` created (healthCheckPath `/health`), `README.md` created. `neurospect-app`: `tsc -b` clean, no changes needed. Both repos ready for Render + Cloudflare Pages dashboard steps. |
| 2026-04-24 | plan | Deployment workstream tracker created at `processes/distributed-workflow/active/deployment.md`. Covers: bundling prompt files into the API repo, gunicorn dep + requirements.txt, DB URL shim, render.yaml, Cloudflare Pages setup, Discord OAuth wiring. Boot prompt written. Prerequisite: Paul creates Discord app in Developer Portal before next session. |
| 2026-04-24 | build | Phase 4 (AI Coach frontend) implemented. Files: `src/types/api.ts` (CoachBias, Confidence, CoachingEventStatus, ChecklistItem, ValidStrategy, Layer3Response, CoachingEvent, TvTokenResponse), `src/lib/constants.ts` (COACH_BIAS_LABELS/STYLES, CONFIDENCE_LABELS/STYLES, STRATEGY_LABELS), `src/hooks/use-coaching.ts` (useLatestCoachingEvent + useCoachingEvent), `src/hooks/use-tv-token.ts` (useTvToken + useRotateTvToken + useRevokeTvToken), `src/components/coach/{bias-badge,confidence-pill,freshness-pill,event-meta,checklist-row,strategy-card,invalid-strategies,alerts-banner,coaching-panel}.tsx`, `src/components/coach-setup/{token-card,pine-script-card,tv-setup-instructions}.tsx`, `src/pages/{coach,coach-setup}.tsx`, updated `src/App.tsx` + `src/components/layout/sidebar.tsx`. Static asset: `public/neurospect-coach.pine` (mirror of wiki canonical). `tsc -b` clean, Vite starts on 5173. Deviation: used `CoachBias` type (not `Bias`) to avoid collision with existing `BiasType`; constants named `COACH_BIAS_*`. Created `concepts/architecture/phase4-coach-frontend.md`. |
| 2026-04-24 | design | Phase 4 (AI Coach frontend) designed and approved. Scope: `/coach` live panel + `/coach/setup` TV token & Pine script page. History deferred (would need new backend list endpoint). Polling: dynamic `refetchInterval` (2s pending, 10s otherwise) via `useLatestCoachingEvent`. Staleness model: visual-only freshness pill (<5m Fresh / 5–30m Stale (Xm) / ≥30m Stale (Xh)). Component breakdown: BiasBadge, ConfidencePill, ChecklistRow, StrategyCard, InvalidStrategies (Collapsible), AlertsBanner, EventMeta, FreshnessPill, CoachingPanel. `STRATEGY_LABELS` map covers all 7 entry models. Pine script distributed as static `public/neurospect-coach.pine` mirror of wiki canonical file. One Sonnet session, no backend changes. Refined Boot Prompt D populated in `ai-coach.md`; canonical `phase4-coach-frontend.md` architecture doc to be created by the implementation session per Architecture Doc Integrity rules. Plan file: `C:\Users\PaulRussell\.claude\plans\playful-juggling-galaxy.md`. |
| 2026-04-23 | build | Phase 3 Session 3 (COMPLETE) — screenshots + analytics dashboard. Fixed `use-analytics.ts` (all 6 breakdown hooks unwrap backend's `{ rows }` / `{ buckets }` wrapper). Created: `src/components/screenshot/{screenshot-upload,screenshot-grid,screenshot-viewer}.tsx`, `src/components/analytics/{summary-cards,breakdown-table,day-of-week-chart,mistake-chart,r-distribution}.tsx`, `src/pages/dashboard.tsx`. Updated: `src/pages/trade-detail.tsx` (replaced placeholder with real screenshots section; PHASE_SETS map drives upload zones per trade status), `src/App.tsx` (DashboardPage replacing placeholder). Phase 3 is COMPLETE — full trade journal MVP: auth, trade CRUD, screenshots, analytics. Created `concepts/architecture/phase3-frontend-structure.md` (canonical frontend doc). Updated `entities/projects/neurospect.md` (Phase 3 complete), `index.md`, tracker. |
| 2026-04-22 | build | AI Coach Phase 3 designed and implemented. Wiki: created `concepts/architecture/tradingview-connector.md` (end-to-end design: Pine Script payload, webhook validation stack, `coaching_events` table, Claude prompt-caching, polling endpoint) and `assets/pine/neurospect-coach.pine` (Pine v5 indicator with hybrid auto/manual 4H FVG, deterministic opens/session detection, rising-edge `alert()`). Backend (`neurospect-api`): Alembic `0002_coach_tables` migration, `tradingview_tokens` + `coaching_events` tables + `coaching_event_status` ENUM, new ORM models, Pydantic Layer 2/3 schemas (extra=forbid on Layer 2), `app/coach/{prompt_loader,validation,claude_client,router}.py`, `app/routers/tv_tokens.py`, config additions (`TRADINGVIEW_WEBHOOK_SECRET`, `TRADINGVIEW_IP_ALLOWLIST`, `PUBLIC_BASE_URL`, `ANTHROPIC_API_KEY`, `CLAUDE_MODEL`, `AI_COACH_PROMPT_DIR`), `.env.example` updates, `anthropic ^0.40` dep. Decisions: hybrid FVG (Pine auto-detect + trader override), on-demand rising-edge toggle, per-user URL-path token, polling channel. Tests: `test_coach_pine_payload.py`, `test_coach_prompt_loader.py`, `test_coach_validation.py`. Updated `processes/distributed-workflow/active/ai-coach.md` session log and `index.md`. Note: Phase 2 router/main/deps/auth scaffolding is a pre-req for the new routers to mount. |
| 2026-04-22 | design | Phase 2 (MVP Backend) project structure designed and approved. Created concepts/architecture/phase2-project-structure.md: directory layout, Poetry deps, auth flow (SPA pattern, Discord OAuth2 + JWT), Alembic sync/async split, analytics SQL approach (streak CTE, unnest, WIDTH_BUCKET), R2 client pattern, file creation order. Updated journal-analytics.md tracker with session log + new boot prompt. Updated index.md. |
|-----------|-----------|---------|
| 2026-04-18 | scaffold | Bootstrapped Neurospect sub-wiki: CLAUDE.md (with Isolation Rule), index.md, log.md, .gitignore, daily/_template.md, daily/2026-04-18.md, processes/distributed-workflow/active/kickoff.md. Folder skeleton created with .gitkeep placeholders. Distributed-workflow pattern docs referenced by absolute path from ALDC wiki (one-way coupling). |
| 2026-04-18 | migrate | Migrated entities/projects/neurospect.md from ALDC wiki into this wiki. Removed ALDC wikilinks (openclaw, ai-driven-dev-workflow) from See Also. Added ICT concept cross-refs and ICT Strategy Context section. |
| 2026-04-18 | create | Created concepts/architecture/transcript-pipeline.md. Decision: manual download + local Whisper (large-v3). Drive API upgrade trigger documented. |
| 2026-04-18 | scaffold | Created 5 ICT concept skeleton pages under concepts/business-logic/: ict-market-structure, ict-liquidity, ict-narratives, ict-entry-models, ict-smt. Frontmatter + section stubs only; body populated by transcript ingest. |
| 2026-04-22 | plan | Spawned 3 new workstream trackers: course-and-kb, ai-coach, journal-analytics. Defined course structure (5 modules, 16 lessons), entry models library (7 strategies with YAML schema), AI coach architecture (TradingView → backend → Claude → JSON response), and ICT trade journal schema. Boot prompts written for all phases. Stream/YouTube transcription in progress (11/13 complete). |
| 2026-04-20 | ingest | Transcribed 12 mentor videos (Whisper medium, CPU) and extracted 8 PDF notes files for Vols 1–4. Saved 20 source files to sources/neurospect/. Fully populated all 5 ICT concept skeleton pages plus created 3 new pages (ict-deviations, ict-order-flow, entities/people/mrwitness-axl). Sources: Vol 1 Classes 1–3 (video + notes), Vol 1 Class 4 (notes only), Vol 2 Classes 1–4 (video for 1,3,4; notes for 1–4), Vol 3 Classes 2,4,5 (video), Vol 4 Classes 1–2 (video). Missing: Vol 1 Class 4 video, Vol 2 Class 2 video, Vol 3 Classes 1,3 video, Vol 4 Class 3 video — confirmed absent from Drive at time of ingest. |
| 2026-04-22 | ingest | Ingested 11 stream transcripts (2026-04-20-stream-*) and 2 YouTube weekly review transcripts (2026-04-22-youtube-*). Created new page ict-live-commentary.md covering: pre-market narrative framework, day-of-week trading protocol, economic calendar protocols (NFP/FOMC/Fed Chair/ISM-PMI/CPI week), HOD/LOD calling process, weekly opening price (Tue/Wed bias), Monday's role in liquidity engineering, speed-and-velocity filter, first presentation FVG, liquidity void, contract rollover, AXL live discipline rules. Updated ict-narratives (weekly OP + Monday engineering + expanded economic calendar), ict-liquidity (liquidity void section), ict-entry-models (OPE order block + propulsion block sequence), ict-smt (trust-the-program framework + NQ/ES/YM trending asset notes). Added 13 new source entries to index.md. |
| 2026-04-22 | build | Course + Entry Models Library session 1 (Module 1 + consolidation model). Created: concepts/course/README.md, module-1-foundations/ (3 lessons), concepts/entry-models/README.md, concepts/entry-models/consolidation-model.md (with YAML). Updated index.md. |
| 2026-04-22 | build | Course + Entry Models Library session 2 (Module 2 + E&R, raid-on-stops, London entry models). Created: concepts/course/module-2-price-delivery/ (4 lessons: APD four-stages, consolidation-model course page, expansion-retracement, reversals), concepts/entry-models/expansion-retracement-model.md (YAML), concepts/entry-models/reversal-raid-on-stops.md (YAML), concepts/entry-models/london-model.md (YAML). Updated index.md with Module 2 and all 4 new entry model entries. Next: Module 3 (session context + bias) + 3 remaining entry models (model-2022-ote, daily-bias, smt-confirmation). |
| 2026-04-22 | build | Course + Entry Models Library session 4 (Module 4 + Module 5 — course complete). Created: concepts/course/module-4-market-structure/ (4 lessons: swing-classification, fractality, structure-deviations, model-2022-ote-csd), concepts/course/module-5-order-flow-and-smt/ (2 lessons: htf-ltf-order-flow, smt-divergence). Updated index.md with Module 4 and 5 sections. Updated course-and-kb tracker session log. **COURSE NOW COMPLETE — all 5 modules, 15 lessons written.** Next: stream/YouTube live commentary integration (optional Module 6 or standalone live-application page). |
| 2026-04-22 | build | Course + Entry Models Library session 3 (Module 3 + 3 remaining entry models). Created: concepts/course/module-3-session-and-bias/ (4 lessons: power-of-three, session-kill-zones, deviations, daily-bias), concepts/entry-models/model-2022-ote.md (YAML with CSD/propulsion block), concepts/entry-models/daily-bias-model.md (YAML — prerequisite filter model), concepts/entry-models/smt-confirmation-entry.md (YAML with Price SMT + PDR SMT). Entry Models Library is now COMPLETE (all 7 models). Updated index.md with Module 3 course section + 3 new entry model entries. Updated course-and-kb tracker session log. Next: Module 4 (market structure, fractality, Model 2022 course page) + Module 5 (order flow, SMT course pages). |
| 2026-04-23 | design | Phase 3 (Frontend MVP) designed and approved. Tech stack: React 19 + TypeScript + Vite 6, TanStack Query v5, React Hook Form + Zod, ky, shadcn/ui (Radix + Tailwind), Recharts, date-fns. Separate repo at `neurospect-app`. Architecture: progressive-fill trade form (3 collapsible sections), URL-driven trade list filters, per-phase screenshot drag-and-drop upload, 7-view analytics dashboard (summary cards, 3 breakdown tables, day-of-week chart, mistake chart, R-distribution histogram). Auth: SPA pattern with Discord OAuth + JWT in localStorage, debug login for dev. 3-session implementation split: (1) scaffold+auth, (2) trade CRUD, (3) screenshots+analytics. Boot prompts written in journal-analytics.md tracker. Frontend architecture doc (`concepts/architecture/phase3-frontend-structure.md`) to be created by Session 3 implementation. |
| 2026-04-22 | build | Phase 1 of Journal & Analytics complete. Created concepts/architecture/trade-schema.md: full ICT trade data model with 3 tables (users, trades, trade_screenshots), 12 Postgres ENUMs, 7 indexes (including GIN for mistake_tags), updated_at trigger, REST API surface (5 CRUD + 3 screenshot + 7 analytics endpoints), schema conventions (UUIDs, TIMESTAMPTZ, soft delete, multi-tenant), future considerations (event sourcing, user-defined setups, multi-leg trades, AI coach integration). Design decisions: single wide trades table, SQLAlchemy async + Alembic ORM, TEXT[] for tags, progressive PATCH updates. Updated journal-analytics.md tracker session log. |
| 2026-04-22 | build | AI Coach Phase 1 + Phase 2 complete. Phase 1: parsed all 7 entry model YAML blocks → generated concepts/ai-coach/strategies.json (version 1.0; 7 strategies with full conditions, itemized checklist arrays with optional flags, stop/target logic, kill_zones, avoid_conditions, _meta block with universal minimum confluence and stop/target conventions). Phase 2: wrote concepts/ai-coach/system-prompt-template.md (version 1.0) — full Claude system prompt with: ICT core rules (daily bias, kill zones, PDA position, entry TF confirmation, HRLR/LRLR, calendar protocols), {{STRATEGY_LIBRARY_JSON}} injection placeholder, 6-step reasoning sequence, strict JSON output schema matching Layer 3 of the ai-coach tracker, design decisions section, testing checklist. Backend language confirmed: Python/FastAPI. TradingView plan confirmed: Pro (webhooks available). Updated index.md (new AI Coach section) and log.md. Next: Phase 3 (Pine Script alert payload + backend webhook spec — enter plan mode). |
| 2026-04-22 | build | Tech stack decision doc written. Created concepts/architecture/tech-stack.md — canonical backend stack spec shared by AI Coach and Journal modules. Pins: Python 3.12 + FastAPI, SQLAlchemy 2.0 async + asyncpg + Alembic, Postgres 16 (Render managed), Pydantic v2, httpx, anthropic SDK, authlib + itsdangerous signed-cookie sessions (JWT deferred), boto3 → Cloudflare R2, gunicorn + UvicornWorker on Render, BackgroundTasks (no Celery/Redis), structlog + optional Sentry, pytest + testcontainers-postgres. Design decisions: Discord snowflake ID is external identifier (discord_id BIGINT UNIQUE) not PK — preserves UUID FK surface of trade-schema; signed-cookie session over JWT for v1; TradingView webhook secured by body-secret + IP allowlist + idempotency key (TradingView does not sign webhooks); Claude call deferred to BackgroundTasks (5–20s latency, avoid webhook timeout); R2 chosen over Render Disk on egress cost, durability, and S3 portability; single web service + managed Postgres topology, no worker. Includes full render.yaml blueprint, env var surface table, ASCII system diagram, OAuth sequence diagram, and bucket key layout. Updated index.md with new tech-stack entry. Next: Journal Phase 2 (MVP backend) and AI Coach Phase 3 (TradingView connector) both boot from this doc. |
| 2026-07-17 | build | Mastery Layer Phase 4 — graded learning roadmap + progress tracker over the Unified Playbook + frontier ICT. Created concepts/mastery/unified/learning-path.md (one sequenced spine U0 psychology → U1 structural primitives → U2 nested SMT stack → U3 execution → U4 risk → U5 frontier confluence-stacking [study-and-watch only] → U6 readiness arc; folds both per-track paths in by reference; each stage carries ladder-stage exit bar + rep target, earlier stages gate later ones) and concepts/mastery/unified/tracker.md (living per-concept + per-frontier-concept grid grouped by U-stage: ladder/confidence/reps; U5 frontier rows tagged with their exact TIER + ESTABLISHED/EMERGING/SPECULATIVE label and marked watch-only, ≤Can-mark; Readiness-to-Live Gate copied by reference — no frontier concept counts toward "core at Backtested+"). Grading model reused by LINK from mastery/README (not restated); drills LINKED to aura/ict-course exercise libraries (never forked); backtesting kept out of scope (U6 references per-track backtest stages + gate). Flagged gap: frontier pages have no dedicated drill library yet — U5 practice is observation overlays on existing session/liquidity drills. Bookkeeping: additive See Also on unified/README + mastery/README; index.md Unified track lines extended + two stale "unified model deferred" lines corrected (Phase 3–4 now built); mastery-layer tracker Phase 4 session log added + marked done. Optional Tier-1 upgrade pass on frontier pages deferred (context) — noted in tracker. Isolation clean (no ALDC refs); no-drift honored. |
| 2026-07-17 | research | Mastery Layer Phase 4 (2nd thrust) — bounded Tier-1 upgrade pass on concepts/advanced/*, via 4 parallel Sonnet research agents (adversarial verify → tier). **CBDR: genuine Tier-1 win** — ICT's own 2016 Premium Mentorship Month 8 "Central Bank Dealers Range" (Lecture 75) + Month 9 "Filling The Numbers" (Lecture 82) verbatim transcripts (quagmyre.com archive; YT IDs nI1AMOC1pro / zanmou0ic5U). Main session independently re-verified Month 8: window "2pm to 8pm New York time"; measurement = candle **BODIES** ("wicks always show erroneous price because of dealing spread") — RESOLVES the prior bodies-vs-wicks open question; size filter <40 pips / 20-30 ideal with his own ⅓-of-~100-pip-ADR rationale. Flout tightened to exact 15:00–24:00 ET. **Contradiction flagged (not smoothed):** Asian-range hours — page said 19:00–00:00 ET; ICT Month-8 implies 20:00 start (at CBDR close) → unresolved, next-session item. **Midnight Open: partial Tier-1** — verbatim "midnight, New York local time… beginning of true day" (ICT 2024 Mentorship Lecture 03, quagmyre; provenance-caveated — community transcriber, video URL unconfirmed); 15:00 close + bias-pivot wording stay Tier-2; 18:00-debunk reinforced with ICT's own NDOG "5pm… resumes 6pm EST" quote. **IPDA: no verbatim upgrade** (Month 5 videos n7SPAK_tpN8 / LRKtiysz4nA located but un-fetchable) — but corrected attribution Months 5,7,8→5,7 (Month 8 is CBDR), trading-days reading favoured (pending verbatim), + flagged an @FlawInTheMatrix/@I_Am_The_ICT rattibha misattribution trap. **Silver Bullet: no upgrade** — transcripts un-fetchable; strategy stays Tier-1 (ICT's own titles), mechanics stay Tier-3; 71%/55-65% win-rates reaffirmed SPECULATIVE. Edited 4 advanced pages + synced unified/learning-path + tracker frontier labels + advanced/README honesty note. Remaining (IPDA verbatim, Silver Bullet mechanics, Asian-range hours) handed to a next-session boot prompt. No commit. |
| 2026-07-18 | research | Mastery Layer Phase 4b — FINISHED the bounded Tier-1 upgrade pass on concepts/advanced/*, via 3 parallel Sonnet research agents (fetch → adversarial verify → tier). Unlock (as predicted): the quagmyre.com archive of ICT's own recorded audio (SRT transcripts + XWiki lecture index). **IPDA: Tier-1 win (partial)** — ICT 2016 Premium Mentorship Month 5 #39 "Quarterly Shifts and IPDA Data Ranges" + #41 "Using IPDA Data Ranges" verbatim: 20/40/60 nested "look back and cast forward" windows CONFIRMED; count is in **trading days** CONFIRMED ("60 trading days 40 trading days and 20 trading days") — resolves the prior calendar-vs-trading open question. The probabilistic **20→40→60 ranking** was searched for and **NOT found** verbatim → stays Tier-3 UNVERIFIED (Month-7 "Blending" unchecked). **Silver Bullet: Tier-1 win + attribution correction** — real primary is ICT's own **2023** Mentorship "ICT Silver Bullet Time Based Trading Model" (YT tRq1hyGGtl4, quagmyre XWiki; corroborated via ictsharks + a tweet tagging @I_Am_The_ICT + Glasp), NOT the 2022 imitator IDs (kG19mCUNdCc/V5iQOKY1J74/RqoMI4wD4lk) this page previously miscited as "ICT's own titled videos" — those are third-party derivatives, now demoted. Three windows (3–4am/10–11am/2–3pm ET) + core sweep→MSS→FVG→retrace(body-inside)→entry→stop-beyond-FVG→target-next-pool sequence CONFIRMED verbatim. The **1–3-min MSS timeframe** and **1:3 R:R** were NOT stated by ICT → stay Tier-3; win-rates stay SPECULATIVE. **Asian-range hours: RESOLVED (Tier-1)** — ICT 2016 Month 8 #74 "Defining The Daily Range" verbatim: "at 8pm eastern standard time… This begins the Asian range. Every day at midnight… ends the Asian range" → **20:00–00:00 ET (4 hr)**, refuting the old 19:00 start; consistent with #75's 8pm-start fragment. Edited all 3 advanced pages + advanced/README honesty note; re-synced unified/learning-path + tracker frontier labels; index.md last_research + Advanced lines + Mastery workstream line updated. All Phase-4b provenance rests on the single quagmyre archive → Tier-1-with-single-archive-corroboration. Only open item: the IPDA 20→40→60 ranking. No commit (Paul handles git). |
| 2026-07-19 | build | Phase 5b — SCAFFOLDED the new `neurospect-learn` app (`C:\Users\PaulRussell\repos\neurospect-learn`, `app/` frontend + `api/` backend, `git init` only — no commit). Executed the approved 5b boot prompt via COPY-not-author from the two lift sources (`neurospect-app` frontend, `neurospect\neurospect-api` backend). **Frontend:** lifted `main.tsx` provider stack, `lib/{api,auth,utils}`, 20 shadcn `ui/` primitives, layout shell, `login`+`auth-callback`; renamed localStorage token key → `neurospect_learn_token`; rewrote `App.tsx` route table over the full taxonomy (`/path`,`/path/:stage`,`/library`,`/concepts/:slug`,`/drills`,`/journal`,`/journal/new`,`/journal/:id`,`/expectancy`,`/gate`; `/`→`/path`) with a generic `StubPage`; new sidebar (Path/Library/Drills/Journal/Expectancy/Gate); `types/api.ts` reduced to auth shapes; rebranded "Neurospect Learn". **Backend:** stripped to the users-only slice — pruned `config` (DB/JWT/Discord/CORS/DEBUG), lifted `database`/`deps`/`auth/{discord,jwt,router}`, added `schemas/auth.py`, `models/{base,user}`+empty `enums.py`, fresh `main.py` (health+auth) and `alembic 0001_initial_users` (trigger fn + `users` + trigger). **Diverged (recorded in learning-platform.md §5b as-built):** (1) lifted `index.css` had no Tailwind-v4 `@theme` block → shadcn semantic utilities weren't emitted; added an `@theme inline` token bridge (verified against built CSS); (2) dropped `recharts` (→5f) + `react-day-picker`/`ui/calendar.tsx` (unused); (3) dropped `boto3`/`anthropic`/`gunicorn`, added `python-dotenv`. **Verified (evidence):** backend `poetry install`+`alembic upgrade head` (users table+trigger on a throwaway PG16 container), `/health`=200, `/docs`, `POST /auth/debug/token`→JWT, `GET /auth/me`→user, no-token→403; frontend `npm install`+`tsc -b`+`vite build` clean, browser: logged-out→`/login`, debug-login→`/path`, six nav items with active highlighting, `/concepts/:slug` renders slug, no console errors. Test infra torn down. Marked 5b ✅ + added session log in learning-platform-ui tracker; updated learning-platform.md (code-is-ground-truth note + §5b as-built). Isolation clean; no commit. Next: 5c data model + migrations. |
| 2026-07-19 | build | Phase 5c — DATA MODEL + MIGRATIONS for `neurospect-learn/api` (four tables, eight fresh Postgres enums, two Alembic migrations, one idempotent seed; git untouched). Executed the approved 5c boot prompt to the established backend idiom (raw-SQL `op.execute`, reuse `update_updated_at()` from 0001, SQLAlchemy 2.0 `Mapped`/`mapped_column` mirroring models/user.py). **Enums** (`app/models/enums.py` + a `pg_enum()` `create_type=False` helper): `u_stage`, `journal_mode`, `entry_model` (7 models + `unified`), `range_position`, `session_type`, `entry_pda`, `outcome`, `grade`. **Models** (registered in `models/__init__.py` + `alembic/env.py`): `concept`, `concept_progress`, `content_page`, `journal_entry`. **Migrations**: `0002_learning_progress` (`u_stage` + `concepts` + `concept_progress` + `content_pages` + updated_at triggers + indexes + a DB-enforced `CHECK (u_stage <> 'U5' OR NOT is_core)` frontier invariant), `0003_journal_entries` (7 journal enums + `journal_entries` + trigger + GIN on `mistake_tags`/`confluence_tags`). **Seed** `api/scripts/seed_concepts.py` — idempotent UPSERT-on-slug, 41 concepts from the U0–U6 taxonomy (learning-path + tracker); `is_core` (10 core) traces to the tracker's `**core**` tags; all 12 U5 rows watch-only carrying their exact TIER/label; `content_slug`+`drill_refs` are soft refs (nullable slug + TEXT[]), never hard FKs. **FINALIZED the model-aligned journal field set** (the load-bearing authoring task): `aura_asset_leg` included (nullable, R7), `entry_pda DEFAULT 'fvg'` (R4), `risk_pct` for the %/R framing; `position_size`/screenshots/`missed_trades` deferred with rationale. **Verified (evidence, local PG16 :5433):** `alembic upgrade head` 0001→0003 clean + `downgrade base` and back up clean (reversible); `\d` confirms 4 tables/8 enums/5 triggers/all constraints/indexes/GIN + FKs (`concept_progress→users,concepts`; `journal_entries→users`); seed→41 rows (0 U5-core, spot-checks OK), re-run idempotent (41 rows/41 distinct slugs); both CHECK constraints reject bad rows; models import; `uvicorn app.main:app`→`/health`=200 with new models registered. Reconciled learning-platform.md §Progress+journal data model → as-built + added §5c as-built; marked 5c ✅ + session log in the tracker; index.md last_build bumped. Did NOT touch trade-schema.md or phase3-frontend-structure.md (other lanes). Isolation clean; no commit. Next: 5d content API + ingest + library/reader. |
| 2026-07-20 | build | Phase 5d — CONTENT API + INGEST + LIBRARY/READER for `neurospect-learn` (git untouched). Executed the approved 5d boot prompt to the established idioms. **Ingest** `api/scripts/ingest_content.py` (async `AsyncSessionLocal` + `pg_insert on_conflict_do_update`, idempotent UPSERT-on-slug + stale-prune, `--dry-run`) parses 67 curated wiki pages (`concepts/{course,entry-models,mastery,advanced,business-logic,aura}/**`) → `content_pages` (body, title from H1, tags/category/source_path, resolved wikilink slugs; tolerant of one malformed-frontmatter file, `aura/aura-asset.md`; READ-ONLY, skips sources/vault). **Slug scheme** (the load-bearing decision): basename + `NN-`-strip + README→dir + leftward de-collision with a **depth tiebreak** → the one real basename collision (`consolidation-model`: entry-model page vs course lesson) resolves to the entry-model page (the concept's "YAML canonical" target); mastery meta-files → `aura-tracker`/`unified-learning-path`/… **Reconciliation: all 27 non-null `concepts.content_slug` soft-refs resolve with ZERO seed edits and NO Alembic 0004** (the 5c `content_pages` shape sufficed; SQL left-join unresolved=0; the 4 intentional NULL synthesis concepts stay NULL). **Backend**: `WIKI_CONTENT_ROOT` config + `.env.example`; `app/schemas/content.py`; `app/routers/content.py` (`/api/content/pages|/pages/{slug}|/search`, all auth-gated via `get_current_user` — content is shared not user-scoped; `/pages/{slug}` returns a badge sourced from the referencing concept since no page carries tier/label in frontmatter); mounted in `main.py`. Dep: `python-frontmatter`. **Frontend**: `react-markdown`+`remark-gfm`+`@tailwindcss/typography`; `lib/content.ts` (TanStack Query hooks + a lookup-based wikilink resolver — backend owns the slug scheme, frontend resolves raw `[[tokens]]` against path/basename maps built from the page list); `components/content/{badges,markdown-renderer}.tsx` (TierBadge/LabelBadge/watch-only; MarkdownRenderer rewrites `[[wikilinks]]`→`/concepts/:slug`, unresolved render inert, display prefers alias else page title); `pages/{library,concept-reader}.tsx`; wired the two 5d routes (replaced stubs). **Playwright harness** (Paul's request, new): `playwright.config.ts` + `e2e/{global-setup,content.spec}.ts` (global-setup mints a debug JWT → storageState; 6 specs pin library/reader/badge/wikilink-click-through/unresolved-inert/search); `npm run test:e2e`. **Verified (evidence, local PG16 :5433 + wiki readable):** ingest→67 rows, re-run idempotent; reconciliation SQL 0 unresolved; endpoints — no-token 403, list 67 grouped, page detail + frontier badge `{U5,Tier 2,EMERGING,watch_only}`, 404 unknown, search hits, `q<2`→422; frontend `tsc -b`+`vite build` clean, no console errors, browser (claude-in-chrome debug-login) — library grouped, reader markdown+table+blockquote, wikilink SPA click-through, unresolved inert, badges, search→4; **Playwright 6/6 green**. Two real bugs caught+fixed in-browser (react-markdown `urlTransform` stripping the `wikilink:` scheme → all links self-pointed; media-based `dark:prose-invert` vs class-based shadcn theme → contrast) + duplicate-H1 wart. Reconciled learning-platform.md §Content delivery → as-built + §5d as-built; marked 5d ✅ + session log in the tracker. Did NOT touch trade-schema.md or phase3-frontend-structure.md (other lanes). Flagged: app's `dark:` variant is media-based while the theme is class-based (latent, inherited from 5b lift). Isolation clean; no commit. Next: 5e progress tracker. |
| 2026-07-20 | design | **Phase 5e — Study Planner + progress layer (plan-mode DESIGN; no app code).** Designed the platform's headline differentiator — an adaptive, gate-aware, retention-aware daily/weekly study-schedule generator — **together with** the minimal progress layer it depends on, so they ship as the 5e arc. Opus main session, plan mode. Read the wiki CLAUDE.md + the `learning-platform-ui` tracker + the design doc `learning-platform.md` in full; read the curriculum the planner schedules against (`mastery/README` ladder/confidence/gate, `unified/learning-path` U0→U6 exit bars, `unified/tracker` grid, both `{aura,ict-course}/exercises.md` drill libraries); ran 2 Explore agents (Sonnet) over the live `neurospect-learn` `api/`+`app/` to pin the build-against idioms. **Put 3 product forks to Paul via AskUserQuestion:** (availability) per-weekday minute budget + max-session + timezone + blackout dates + optional target date; (horizon) plan to each stage gate with a **pacing-only ETA** — a go-live date NEVER gates (the evidence-based Readiness Gate stands); (build split) deferred to me → **3 sub-phases** 5e-1 progress foundation / 5e-2 planner engine / 5e-3 planner UI. **Engineering calls:** hybrid persist-vs-compute (future computed on read; past+today **frozen** into `plan_items` for a real adherence record → "fixed accountable past, always-recomputed future"); a **deterministic gate-aware scheduler** (unlock → backlog → freetext rep-target parse → mandatory spaced review → daily packing → ETA projection → slippage carry-over; same state → same plan); a `drills` catalog **seeded from the two `exercises.md` drill-map tables**; a `rep_targets` parser that defaults conservatively and never invents targets. **Data model (Alembic 0004, designed):** `study_preferences`, `plan_items`, `drill_progress` (user-scoped, soft-deleted, partial-unique `WHERE NOT is_deleted` like `concept_progress`) + a `drills` seed table; 3 enums (`plan_activity`/`plan_item_status`/`drill_variant`); `concepts`+`concept_progress` unchanged. **North star enforced by design:** prescriptive Today (skips logged not hidden), gated progression (locked stages never scheduled; ladder-advance blocked until reps≥target+conf; no gate override; U5 never live-eligible), rep targets enforced, adherence/streak/days-behind surfaced, mandatory spaced review, U0 habits recur daily, missed work re-queued, target date pacing-only. **Surfaced from the code survey:** planner introduces the **first `useMutation`** (5b–5d read-only); `progress`/`calendar`/`slider` shadcn + `recharts`/`react-day-picker` not present (calendar → lightweight custom CSS-grid + `date-fns`); the `dark:` media-vs-class mismatch to fix in 5e-1. **Flagged (integrity):** stage exit-bar rules live in code (`services/stages.py`) as as-implemented — reconcile if learning-path changes; U0/U4 gates partly self-attested until 5f/5g; freetext `rep_target` parser + optional future structured seed column. **Did:** extended the ONE canonical doc `learning-platform.md` (§Study Planner + folded 5e into §data model / §route taxonomy / §component+API surface + revised §implementation split 5e→5e-1/2/3); redefined Phase 5e in the tracker + marked the design boot prompt ✅ EXECUTED + added a session-log entry; additive cross-links from `mastery/README` + `unified/learning-path`; index.md (`updated`/`last_design` + 2 catalog lines). **Verified (design-session = doc integrity):** isolation clean (no ALDC refs); no-drift (design LINKS the curriculum + extends the single canonical doc, no competing doc, no restated curriculum); frontier watch-only/never-live-eligible preserved; all 7 boot-prompt decision areas covered; no app code written. Paul handles git — no commit. Next: Phase 5e-1 build (progress foundation). |
| 2026-07-20 | build | **Phase 5e-1 — PROGRESS FOUNDATION for `neurospect-learn`** (git untouched). Executed the approved 5e-1 boot prompt to the established idioms. **Migration** `alembic/versions/0004_drills_progress.py` (raw-SQL, reversible, reuses `update_updated_at()`): `drill_variant` enum (`hand\|tool`) + `drills` (seed/content, no soft-delete; `drill_ref` UNIQUE, `track` CHECK-VARCHAR `aura\|ict_course`, `stage_code`, `title`, `advances_to`, `rep_target` freetext, `concept_slugs` TEXT[] + GIN) + `drill_progress` (user-scoped, soft-deleted; `reps`/`hand_done`/`tool_done`/`last_practiced`/`notes`, `UNIQUE(user_id,drill_ref) WHERE NOT is_deleted`). **Models** `app/models/{drill,drill_progress}.py` (registered in `models/__init__.py` + `alembic/env.py`). **Seed** `scripts/seed_drills.py` — parses the "Drill → concept → ladder-stage map" tables at the foot of both `mastery/{aura,ict-course}/exercises.md`, expands compound refs (`D0-a…e`, `D4-a/b`, `D6-a/b/c`, `T-01…14`, `Stage 7`→`S7`) to atomic `drill_ref`s matching `concepts.drill_refs`, `concept_slugs` reverse-derived from the concept seed → **53 drills** (aura 16, ict_course 37; the 4 aura Stage-0 drills the aura map omits reported as orphan refs), idempotent on `drill_ref` + stale-prune. **Services** (pure): `rep_targets.py` (freetext→(kind,count): reps/days/sessions/qualitative/habit; largest floor binds; identifier-embedded numbers like "Class-2" excluded via lookbehind; never invents a target) + `stages.py` (computed-never-stored exit-bar gate encoding unified/learning-path §Stage gate as the as-implemented gate; splits `auto_met` from `met`, locks off `auto_met` so U0/U4 un-wired attests don't freeze the path; U5 watch-only/never-gate-eligible; U6=5g placeholder). **Router** `app/routers/learning.py` (`/api`, auth-gated + user-scoped; schemas in `app/schemas/learning.py`; mounted in main.py): `GET concepts` (`?stage=`), `GET/PATCH progress` (lazy upsert + ladder-advance enforcement [reps≥target + confidence set] + watch-only Can-mark cap), `GET stages`, `GET drills` (`?track=`/`?stage=`), `PATCH drills`. **Frontend**: `lib/learning.ts` (first `useMutation`s — `learningKeys` + `useUpdateProgress`[invalidates progress+stages]/`useUpdateDrill`; surfaces the 422 gate msg); `types/api.ts` learning types; components `progress/{StagePath+StageNode[SVG ring],ExitBarGate,LadderBadge,ConfidenceRating,RepCounter,ConceptTrackPanel}` + `drills/DrillCard`(✋/🛠); pages `/path`,`/path/:stage`,`/drills` (replaced stubs) + the track panel on the reader; `shadcn add progress`; added `@custom-variant dark` (the §5d-flagged fix). **Verified (evidence, local PG16 :5433 + wiki):** `alembic` 0001→0004 up/down/up clean (reversible); seed_drills 53 rows, re-run idempotent; **API 15/15** (self-written harness: lazy create, re-PATCH in-place, per-user isolation, CHECK/validation reject ladder=9/conf=9, ladder-advance enforcement rejects premature Can-mark, U1 gate flip met↔not on the 5-primitive fixture, U5 never-gate-eligible, drill persist, 404, no-token 403); `tsc -b`+`vite build` clean; **Playwright 10/10** (6 content + 4 new learning: path spine, reader edit-persist, stage exit-bar flip, drill mark); browser (claude-in-chrome debug-login): `/path` spine w/ rings + locked/unlocked + watch-only + attestation-pending, `/path/U1` exit-bar checklist + editable track panel, `/drills` cards w/ ✋/🛠 + rep counters + back-links, no console errors. **Two real fixes caught in verify:** ON CONFLICT partial-index predicate (`IS false`≠`NOT is_deleted` → `text("NOT is_deleted")`) and stale-ORM-read on PATCH response (build from committed values, not re-SELECT under `expire_on_commit=False`); + a UX fix (ExitBarGate shows true bar status even when locked). Reconciled `learning-platform.md` §Progress data model §4 + §Component/API surface → as-built + added §5e-1 as-built + marked 5e-1 ✅ in the split; marked 5e-1 ✅ + session log in the tracker; index.md last_build bumped. Did NOT touch trade-schema.md or phase3-frontend-structure.md (other lanes). Isolation clean; no commit. Next: 5e-2 planner engine (`study_preferences`+`plan_items`/Alembic 0005 + scheduler + planner API). |
| 2026-07-20 | design | **Phase 5e-1b — MULTI-TRACK CURRICULUM (path redefinition; wiki design only, no app code).** On reviewing the shipped 5e-1 `/path`, Paul judged it low-value (a bare progress grid, not a curriculum) and redefined the path: **three first-class graded tracks** (Aura · AXL/MrWitness · Unified) with a switcher, instead of one unified spine. Design dialogue (2 AskUserQuestion rounds) settled: **parallel tracks**, **separate per-track progress** (a shared primitive is duplicated on purpose — more reps + a second mentor's explanation), **cross-links** between equivalent concepts across tracks ("Also taught in: …"), each stage a **curriculum unit** (Read → Drill → Track → Gate), and **build this before the planner** (the planner schedules whichever track is active). **Engineering shape set:** `concepts` gains `track` (aura\|ict_course\|unified) + `stage_code` + `stage_order` + `cross_refs` (u_stage → nullable, unified-only); new `track_stages` seed table (title/summary/gate_text per stage); seed grows 41 → ~72 (author ~14 Aura + ~17 AXL concept rows faithfully from `aura/learning-path.md` + `course/README.md`, distinct slugs, own content_slug/drill_refs/is_core/rep_target/cross_refs); `stages.py` generalizes to a data-driven per-stage gate (unified U0–U4 exact rules as a special case); new `GET /api/tracks` + `/path/:track/:stage`; Alembic `0005` (planner bumped to `0006`). **Did:** extended the canonical doc `learning-platform.md` (§Multi-track path redefinition + §Multi-track data model + rewritten §Route taxonomy + 5e-1b inserted in §Implementation split); added the Decisions block + 5e-1b Plan entry + a full **5e-1b build boot prompt** + a session-log entry in the `learning-platform-ui` tracker; bumped index.md `last_design`. **Verified (design integrity):** read both per-track curricula to confirm the seed mapping is a faithful projection (no invention; gaps to be flagged at build); isolation clean (no ALDC); no-drift (tracks CONSUME/LINK the wiki paths, never restate). Did NOT touch trade-schema.md / phase3-frontend-structure.md. Recommended the 5e-1b code build run as a fresh session (this session already shipped 5e-1 end-to-end). Paul handles git — no commit. Next: execute 5e-1b build, then 5e-2 planner. |
| 2026-07-21 | build | **Phase 5e-1b — MULTI-TRACK CURRICULUM for `neurospect-learn`** (git untouched). Executed the approved 5e-1b boot prompt (Opus, no plan mode) to the established idioms. **Migration** `alembic/versions/0005_multi_track.py` (raw-SQL, reversible, reuses `update_updated_at()`): `concepts` +`track` VARCHAR CHECK `(aura\|ict_course\|unified)` DEFAULT `'unified'`, +`stage_code`/`stage_order`/`cross_refs`, `u_stage` → nullable (unified-only); NEW `track_stages` (seed/content, no soft-delete: `track`/`stage_order`/`stage_code`/`title`/`summary`/`gate_text` + trigger + UNIQUE `(track,stage_code)`) — **metadata only, NO `concept_slugs`** (a stage's concepts/drills group on `concepts.(track,stage_code)` + `concept.drill_refs`, anti-drift). Downgrade clears dependent `concept_progress` before deleting non-unified concepts (FK fix). **Model** `app/models/track_stage.py` + `concept.py` extended (registered in `models/__init__.py` + `alembic/env.py`). **Seeds**: `seed_concepts.py` (unified 41 backfilled track/stage_code/stage_order; +14 Aura A0–A3 + 19 AXL M0–M5 concept rows from `aura/learning-path.md` + `course/README.md`, distinct slugs, own content_slug/drill_refs/is_core/rep_target; `cross_refs` assigned from a validated `EQUIV_GROUPS` table — symmetric, never same-track, raises on unknown slug) → **74 concepts**; NEW `seed_tracks.py` → **23 track_stages** (aura A0–A6=7, ict_course M0–M8=9, unified U0–U6=7) with title/summary/gate_text from the learning-path pages; `seed_drills.py` re-pointed `concept_slugs` to same-track (keyed by `(track,drill_ref)`) → 53 drills unchanged. **Service** `stages.py` generalized: `compute_stages(track, stage_metas, concepts, progress)` — unified keeps its **exact** U0–U6 rules; aura/ict use the generic rule (foundation stage = every concept ≥Can-mark + attest; concept stage = `is_core` ≥Can-mark + conf≥3 + reps≥floor; concept-less stage = attest placeholder); per-track lock chains (unified exact; aura/ict sequential auto_met). **Endpoints** `learning.py`: NEW `GET /api/tracks` (3 tracks + per-stage rollups), `GET /api/stages?track=` (default unified, 404 unknown), `?track=` on `/concepts` + `/progress`; PATCH unchanged; schemas split `StageRollup`/`StageOut` + new `TrackOut`, `ConceptOut`/`ProgressRow` gained track/stage_code/stage_order/cross_refs. **Frontend**: `/path` track switcher (Aura·AXL·Unified, default Aura, localStorage-persisted) → selected track's `StagePath` (keyed on track/stage_code); `/path/:track/:stage` reshaped into the **curriculum unit** (Read = content_slug links · Drill = DrillCards for the stage concepts' drill_refs · Track = ConceptTrackPanels · Gate = gate_text + ExitBarGate); `ConceptTrackPanel` "Also taught in: {Track} — {title}" cross-links via new `useConceptIndex()`; `lib/learning.ts` `useTracks`/`useConceptIndex`/track-scoped `useStages`/`useProgress` + widened invalidations; `types/api.ts` updated. **Verified (evidence, local PG16 :5433 + wiki):** alembic `0001→base→head` up/down/up clean (caught+fixed the downgrade FK bug); all 3 seeds idempotent + per-track counts reported; SQL — **0 unresolved content_slug, 0 unresolved cross_ref, 0 orphan concept stages, 0 cross-track drill links, 44 concepts carry cross_refs**; `GET /api/tracks`→3 tracks w/ their stages; **per-track isolation proven** (marking Aura A1's 3 cores flips only Aura A1 met — Unified U1 0/5 + ICT M1 0/2 untouched, unified `u1-1` stays None); gate flips per track; `tsc -b`+`vite build` clean; **Playwright 12/12 green** (6 content + 6 multi-track: track switch, curriculum-unit Read→Drill→Track→Gate, reader edit-persist, per-track isolation, cross-link jumps tracks, drill-mark). The live claude-in-chrome visual walkthrough was NOT separately run (Chromium e2e covers the behaviours). **Ops (not code):** mid-verify Docker Desktop stopped (DB dropped) + an earlier full `downgrade base` had emptied `content_pages`; restarted Docker + container, re-ran all seeds **and `ingest_content` (67 pages)**; cleaned up stray uvicorns (one runs on :8000 w/ current code). Reconciled `learning-platform.md` §Multi-track data model + §Route taxonomy → as-built + added §5e-1b as-built + marked 5e-1b ✅ in the split + top banner; marked 5e-1b ✅ + session log in the tracker (boot prompt → ✅ EXECUTED). Did NOT touch trade-schema.md or phase3-frontend-structure.md. Isolation clean; no commit. Next: 5e-2 planner engine (`study_preferences`+`plan_items`/Alembic 0006 + scheduler + planner API). |
| 2026-07-22 | build | **Phase 5e-2 — STUDY PLANNER ENGINE for `neurospect-learn`** (backend only — no UI; git untouched). Executed the approved 5e-2 boot prompt (Opus, no plan mode) to the established idioms. STEP 0 passed (alembic 0005, seeds 74/23/53/67, `/api/tracks`=3, `/stages?track=aura`=A0–A6, frontend build clean). **Migration** `alembic/versions/0006_study_planner.py` (raw-SQL, reversible, reuses `update_updated_at()`): NEW enums `plan_activity` (`learn\|drill\|review\|observe\|habit\|backtest`) + `plan_item_status` (`pending\|done\|partial\|skipped`); **`drill_variant` reused from 0004** (not recreated); `study_preferences` (user-scoped, soft-deleted, `UNIQUE(user_id) WHERE NOT is_deleted`; timezone, 7 weekday minute budgets, max_session_minutes, blackout_dates, target_go_live_date [pacing-only], **`active_track` CHECK aura\|ict_course\|unified** per the 5e-1b reconciliation, plan_version, generated_at) + `plan_items` (user-scoped, soft-deleted; plan_version/scheduled_date/activity/concept_id[FK]/drill_ref/drill_variant/target_qty/target_unit/est_minutes/status/done_qty/completed_at/sort_order; index `(user_id,scheduled_date)`, partial-unique slot `(user_id,scheduled_date,activity,concept_id,drill_ref,drill_variant) **NULLS NOT DISTINCT** WHERE NOT is_deleted` so a concept-less all-NULL `backtest` item still de-dupes on materialize). **Models** `app/models/{study_preferences,plan_item}.py` (registered in `models/__init__.py`+`alembic/env.py`; `plan_item.drill_variant` binds the 0004 enum `create_type=False`). **Service** (pure, DB-agnostic, deterministic — no clock/randomness/DB) `app/services/scheduler.py`: `schedule(today, prefs, stage_metas, concepts, drills, concept_progress, drill_progress, past_items, *, horizon_days)` → `ScheduleResult` (dated items + projected_clear/projected_go_live/on_pace + days_behind/carried_over + unplaced). Pipeline: unlock via `stages.compute_stages(prefs.active_track,…)` (locked stages NEVER scheduled) → backlog (untracked→Learn; each drill below its `rep_targets.parse` floor→Drill [count-y=one task carrying remaining reps; days/sessions=one session/day]; watch_only/U5→Observe capped at Can-mark; foundation stage_order 0→Learn/Drill **plus** a recurring daily Habit; lowest unlocked concept-less stage→a single Backtest placeholder) → mandatory spaced review (Leitner conf 1→1d…3→3d,4→7d,5→21d, None→3d) → deterministic daily packing (tz-resolved today; skip blackout+0-budget days; fill habits→due reviews→backlog in curriculum order; day-based drills one slot/day; est-minutes capped at max_session; overflow rolls; habits mandatory) → projection/ETA (pacing-only vs target_go_live_date — never gates) → slippage carry-over (past pending re-queued at FRONT of today; days_behind/carried_over surfaced; unplaced never dropped). **Router** `app/routers/planner.py` (`/api`, auth-gated + user-scoped; schemas `app/schemas/planner.py`; mounted in main.py): `GET\|PUT /preferences` (defaults w/ `is_configured=false` when unset), `GET /plan/today` (resolves today in user tz — the one clock read; runs scheduler; **materializes today via `pg_insert on_conflict_do_update` on the slot index `WHERE status='pending'`** — never clobbers a mark; returns frozen rows + adherence + pace), `GET /plan?from=&to=` (past/today frozen [id set] + future computed [id null]), `POST /plan/regenerate` (bumps plan_version+generated_at, soft-deletes today's PENDING [done/partial/skipped kept], re-materializes), `PATCH /plan/items/{id}` (status + done_qty → feeds concept_progress/drill_progress reps+last_practiced via the lazy upsert; NEVER advances the ladder — that gate stays with `/api/progress`). **Verified (evidence, local PG16 :5433):** alembic `0001→head→base→head` clean on a **throwaway** DB + `0006` down/up on the working DB (seed + `drill_variant` preserved); **9 scheduler unit tests with NO DB** (determinism, per-track unlock, locked-never-scheduled, U5 observe-only+capped, blackout/0-budget skips, max-session cap, spaced-review due dates, slippage re-queue, pacing-only projection); **7 API tests** (prefs round-trip, `/plan/today` idempotent [2nd call 0 dupes], PATCH feeds concept + drill progress, regenerate bumps plan_version + preserves a done item, per-user isolation, 5e-1b `/tracks`+`/stages` no-regression) — 16 green; live uvicorn boot: all 5 planner routes mount, `PUT prefs → GET /plan/today` returns tz-aware habits/learn + adherence + pace; **Playwright 12/12 green** (5e-1b regression). API tests use a dedicated `NullPool` engine + `dependency_overrides[get_db]` (module-global engine pools connections across pytest's per-test event loops → "Event loop is closed"). **Ops (evidence-gating lesson):** a scratch `downgrade base→upgrade head` intended for a throwaway DB ran against the **working** DB (an exported `DATABASE_URL` did not override the `.env`-derived settings Alembic reads), wiping the seed; recovered by re-running all seeds + `ingest_content` (74/23/53/67 restored). Never run `downgrade base` against the working DB. Reconciled `learning-platform.md` §Study Planner + §Progress data model §4 → as-built (`0005`→`0006`, `active_track`, `NULLS NOT DISTINCT`, track-scoped signature) + added §5e-2 as-built + marked 5e-2 ✅ in the split; marked 5e-2 ✅ + session log in the tracker (boot prompt → ✅ EXECUTED). Did NOT touch trade-schema.md or phase3-frontend-structure.md. Isolation clean; no commit. Next: 5e-3 planner UI (`/today`·`/plan`·`/plan/setup`). |
| 2026-07-23 | build | **Phase 5e-3 — STUDY PLANNER UI for `neurospect-learn`** (frontend only — consumes the 5e-2 API unchanged; git untouched). Executed the approved 5e-3 boot prompt (Opus, no plan mode). STEP 0 passed (alembic `0006 (head)`; seeds 74/23/53/67; planner API live — prefs round-trip + `/plan/today` items>0 + range + regenerate + patch all respond; backend `pytest` 16/16; frontend `tsc -b`+`vite build` clean + Playwright 12/12). Read the design contract (§Study Planner + §Route taxonomy + §Component/API surface + §5e-2 as-built — the exact `schemas/planner.py` Pydantic shapes) + the live frontend idioms (`lib/learning.ts` query/mutation pattern, `App.tsx`/`sidebar.tsx`, the 5e-1 components + e2e harness). **Built:** `types/api.ts` planner types (mirror `schemas/planner.py`); `lib/planner.ts` — `plannerKeys` + `usePreferences`/`useUpdatePreferences`/`usePlanToday`/`usePlanRange`/`useUpdatePlanItem`/`useRegenerate` (**a mark invalidates BOTH `plannerKeys.all` + `learningKeys.all`** since it feeds progress); `components/planner/` — `TodayList`, `PlanItemCard` (activity icon + concept/drill title + target + est-minutes + done/partial/skip → PATCH; carried-over + status styling; **computed future items [`id===null`] render read-only**), `StudyCalendar` (a **custom Mon-first CSS-grid month calendar** via `date-fns` — per-day status dots + count, today ring-highlit, past frozen / future projected, Regenerate + `unplaced` warning; **`react-day-picker` NOT re-added**), `AvailabilityForm` (RHF+Zod — weekday minutes, max-session, timezone auto-defaulted from `Intl`, `active_track` Select, blackout chips [local-state, merged on submit], pacing-only target date), `StreakBadge`/`AdherenceMeter`/`PaceProjection`; pages `/today`·`/plan`·`/plan/setup` + the 3 routes in `App.tsx` + Today/Plan in `sidebar.tsx`. **Bug caught + fixed IN THE LIVE BROWSER:** `AdherenceMeter` multiplied `adherence_pct` by 100 → rendered **2500%**; the backend already returns a 0–100 percentage (`planner.py`: `round(100*(done+0.5·partial)/total,1)`; the schema comment omits the ×100) — fixed to render directly + corrected the `types/api.ts` comment. **Verified (evidence):** `tsc -b`+`vite build` clean; **Playwright 17/17** (12 existing regression + 5 new planner: setup→ordered non-empty Today; mark-done flips `data-status` + surfaces adherence + **feeds progress** via `/api/progress` cross-check; skip logs a SKIP; calendar shows today's dots; regenerate bumps `plan_version`) — planner specs run **serially against their own isolated debug user** (`addInitScript`) so they don't race with `learning.spec`; live claude-in-chrome walkthrough (debug-login → `/plan/setup` save → `/today` mark-done + skip → `/plan` calendar → regenerate) with **NO console errors** (the 2500% bug was caught here). Ops note: the API `CORS_ORIGINS` allows only `:5173` — the dev server must run there for `/auth/me` to pass. Reconciled `learning-platform.md` §Study Planner header + §Route taxonomy + §Component/API surface → as-built + added §5e-3 as-built + marked 5e-3 ✅ in the split; marked 5e-3 ✅ + session log in the tracker (boot prompt → ✅ EXECUTED). Did NOT touch trade-schema.md or phase3-frontend-structure.md. Isolation clean; no commit. Next: **Phase 5f — Journal + expectancy** (model-aligned `/journal` [backtest\|live] + `/expectancy` dashboard; recharts re-added). |
| 2026-07-24 | build | **Phase 5g — READINESS-TO-LIVE GATE for `neurospect-learn`** (git untouched) — **COMPLETES THE PHASE 5 ARC**. Executed the approved 5g boot prompt (Opus, no plan mode). STEP 0 passed (alembic `0006 (head)`; seeds 74/23/53/67; backend 33 tests; `tsc -b`+`vite build` clean; Playwright 20/20). Read the contract (§Progress+journal data model §3 + §Route taxonomy + §Component/API surface + §5e-1/§5f as-built) + the canonical Gate in `mastery/README` §Readiness-to-Live Gate, then the live code it combines. **Migration** `alembic/versions/0007_gate_attestations.py` (raw-SQL, reversible, reuses `update_updated_at()`): NEW enum `gate_attestation_item` (`risk_precommitted\|sim_track_record\|journaling_habit\|circuit_breaker`) + `gate_attestations` (user-scoped, soft-deleted, `attested`/`attested_at`/`note`, `UNIQUE(user_id,item) WHERE NOT is_deleted` — the lazy-upsert target). **Deliberately NO `cleared` column anywhere** — the verdict is computed per read. **Model** `app/models/gate_attestation.py` (registered in `models/__init__.py`+`alembic/env.py`). **Service** (pure, DB-agnostic) `app/services/gate.py`: `compute_readiness(concepts, ladder, groups, attested, …, credit_track, models)` → per-model `cleared` = (a) ∧ (b) ∧ (c) with per-requirement `met`/`detail` + human-readable `blocking`. **(a)** anchored on the **unified** curriculum (the only track whose taxonomy enumerates all 7 entry models, as U3.2a–g — mapped by **explicit slug** in `ENTRY_MODEL_CONCEPTS`, the `stages._U2_GATE_SLUGS` idiom): its 10 `is_core` U1–U4 concepts at **Backtested+ (ladder≥3)** + the model's own entry-model concept at **Live-ready (4)**; **`unified` requires all seven at Live-ready** (it routes into every model — the advanced track carries the hardest bar). Cross-track `cross_refs` equivalents **supply credit** (Aura study of the same primitive is real evidence); `credit_track` restricts the candidate set so it can only ever **TIGHTEN** — no caller parameter can loosen a verdict. **(b)** the **REUSED** 5f pure `expectancy.compute_groups` (called exactly as the analytics router does, nothing reimplemented): sample ≥ `SAMPLE_TARGET` (50) closed **backtest** trades · expectancy strictly `>0` · win rate ≥ break-even for its planned R:R (unknown R:R fails it — "a win rate without its R:R is meaningless"); `SAMPLE_STRETCH=100` (README's "ideally") surfaced, **never gating**; live trades never gate (a losing live record is shown for honesty). **(c)** **all four** README behavioural items (not the boot prompt's three), per-user + revocable, with **objective journal corroboration** (entries logged / distinct journaling days / backtest-live split / last entry) shown beside them rather than invented thresholds. **Fails closed:** a missing curriculum concept or an empty core set emits an UNMET requirement (an empty list would let `all()` report cleared). **Frontier** (`watch_only`) is neither a requirement nor a creditor, and is returned separately as never-gate-eligible; `confluence_tags` unread. **Router** `app/routers/gate.py` (`/api`, auth-gated + user-scoped; schemas `app/schemas/gate.py`; mounted in main.py): `GET /api/gate?track=`, `GET\|PATCH /api/gate/attestations`. **Frontend**: gate types in `types/api.ts`; `lib/gate.ts` (`gateKeys` + `useGate`/`useAttestations`/`useUpdateAttestation`); `components/gate/{gate-signal,gate-checklist}.tsx` (`GateSignal` = per-model signal + 3 source pills + n/expectancy/win-vs-BE + blocking list; `GateChecklist` = the 3 groups, concept/evidence rows read-only *earned* w/ a "work it" link to `/path/:track/:stage`, the 4 attestations the page's **only** writable control — server-controlled Radix checkbox, **deliberately not optimistic**); `pages/gate.tsx` (banner · signal grid · checklist · frontier panel · credit-track selector) replaced the last stub — **`pages/stub.tsx` DELETED, every route in the taxonomy now implemented**. **Verified (evidence, local PG16 :5433):** `0007` up + `downgrade 0006` + up clean (table+enum dropped/recreated; 74-concept seed preserved); **68 backend tests** (33 prior + **24 pure hand-fixture gate units** + **11 gate API**) — the cleared case then **each of the four inputs flipped** with the right blocking reason (incl. positive-expectancy-but-below-break-even, unknown R:R, and each attestation), frontier never a requirement nor creditor, cross-track credit, `credit_track` tightening both ways, unified-needs-all-seven, missing/empty curriculum fails closed, `ALL_MODELS` ≡ the `EntryModel` enum (drift guard), `cleared` ≡ the 3-source conjunction; the API suite clears a **fully satisfied London against the real seed** then flips all four back, plus attest round-trip/revoke (one row, upserted in place), **non-overridability** (all four attested + full ladder + no sample → still blocked; a stray `cleared` field in the body ignored), per-user isolation, corroboration, 404/422/403. `tsc -b`+`vite build` clean. **Playwright 27/27** (20 prior regression + 7 new gate). Live claude-in-chrome walkthrough: London **Cleared** (50/50, +0.80R, win 60% vs BE 33%) while all seven other models stayed blocked **with all four discipline boxes ticked** — non-overridability visible on the rendered surface; checklist 11/11·3/3·4/4; frontier panel (12 concepts); credit-track selector toggled to "Aura only" and back, flipping London Cleared→Not cleared→Cleared; **no console errors**; one copy wart caught in-browser and fixed ("across 1 days"→"1 day"). Reconciled `learning-platform.md` §3 (the gate → as-built) + §Route taxonomy + §Component/API surface + §Stage exit-bar derivation (U6 now points at `/gate`) + the **stale top banner** (it still claimed the Study Planner was unbuilt though 5e-2/5e-3/5f had shipped) + added §5g as-built + marked 5g ✅; marked 5g ✅ + a session log + the arc-complete status banner in the tracker (boot prompt → ✅ EXECUTED). Also fixed 3 now-misleading in-app strings that forward-referenced "Phase 5f/5g" as unbuilt (`stages.py` ×2, `exit-bar-gate.tsx`) — copy only, no logic change. Did NOT touch trade-schema.md or phase3-frontend-structure.md. Isolation clean; no commit. Ops: the stale uvicorn on :8000 predated the gate router and was restarted. **Next: Phase 5 is COMPLETE — no ⏭ ACTIVE boot prompt.** Candidate follow-ups (Paul's call, none scoped): wire U4's stage attestation to the shipped `services/gate.py`; the deferred journal items (screenshots, `position_size`, Aura `missed_trades`); live-trading/commentary; deploy/hosting. |
| 2026-07-25 | design | **Phase 6 boot prompt + a NEW `learning-enforcement` workstream (wiki only, no app code).** Post-5g direction-setting with Paul (2 AskUserQuestion rounds). **Decided (Paul):** (1) the next session is **Phase 6 — Phase-5 debt**, scoped to exactly three items — **6a** wire the `services/stages.py` attestations to the evidence that now exists (the 5g `gate_attestations` store + the 5f expectancy service) so `/path` stops showing permanently-unmet placeholder rows; **6b** the deferred **missed-trade log** (Aura canceled orders — the "how much is hesitation costing you?" opportunity-cost analytic), adapted to the learn app's model-aligned conventions (`entry_model`/`session_type`, the `journal_entries` idiom) rather than copied from the `trades` schema, and **without** the `missed_trade_screenshots` child table; **6c** `position_size` + small journal gaps with expectancy staying R-based. **Deploy/hosting explicitly OUT.** (2) The grading/anti-cheat/gamification vision gets **its own tracker**, not Phase 6+ of the completed Phase 5 workstream. **My recommendation Paul accepted: screenshots/evidence storage stay deferred on purpose** — the 5c journal screenshots and `missed_trade_screenshots` are the *same primitive* the drill-grading vision needs (user-uploaded evidence of chart markings, attached to a concept and judged), so building either now would pre-commit its shape (what it attaches to, grading state, who verifies) before the grading model exists. **Did:** wrote the full **Phase 6 ⏭ ACTIVE boot prompt** in `learning-platform-ui.md` (STEP 0 against the 5g baseline: alembic `0007`, seeds 74/23/53/67, 68 backend tests, Playwright 27/27; scope 6a/6b/6c with the calls to DECIDE + RECORD — whether a wired attest feeds `auto_met`/the lock chain [recommended: keep `locked` on the concept chain, make `met` honest], where the missed-trade surface lives [recommended: inside `/journal`], one-vs-two migrations; **evidence-gate = capture `/api/analytics/expectancy` + `/api/gate` before/after and show them byte-identical**, since 6b/6c add data that must not move expectancy or the verdict; `stages.py` must stay pure + the 5e-2 scheduler must keep working; do NOT edit `trade-schema.md`), plus a 2026-07-25 Decisions block, a Phase 6 Plan entry, and a rewritten status banner. **Created `processes/distributed-workflow/active/learning-enforcement.md`** — goal in Paul's own framing (verified drills via uploaded screenshots of markings · anti-cheat · gamification for consistency · plus a pass on the learning path's structure and drill quality); a **sharpened north star** (*the adversary is self-deception, not an attacker — so make the honest path the path of least resistance and any shortcut visible rather than impossible; a design that merely frustrates honest work fails as badly as one that lets a rep be faked*); an inventory of what already enforces the process (non-overridable gate, gated ladder advance, watch-only cap, prescriptive planner + logged skips, backtest/live separation) **and the honest gap it closes — every one of those ultimately trusts a self-reported `reps` integer and a self-rated confidence**; **14 open questions** for the research session across evidence/grading (unit of evidence, how a marking is graded, where rubrics come from, blocking vs async, score vs pass-fail), anti-cheat (which shortcuts to close vs merely surface; the appeal path), gamification (which mechanics actually drive solo consistency; **the failure mode: rewarding activity over mastery would undermine the gate**), storage/platform (R2, whether deploy becomes a dependency), and curriculum; the **content-vs-app scope boundary** (a drill can only be graded if its *wiki* definition states a gradable bar, so the content pass likely leads); and the inherited debt it owns. Phase E1 = an **Opus 5 deep-research + design session**, **boot prompt authored after Phase 6 lands** (Paul's sequencing, so the research starts from real post-Phase-6 code). Verified all 5 wikilink targets exist; isolation clean (Neurospect-only; ALDC pattern docs referenced by absolute path only). Updated index.md (new tracker entry + corrected the learning-platform-ui line + `last_design`). Paul handles git — no commit. Next: execute the Phase 6 boot prompt. |
