---
date: 2026-04-24
---

# Daily Notes — 2026-04-24

## Note
type: architecture
ref: phase4-coach-frontend

Phase 4 (AI Coach frontend) fully implemented and verified today.

**What was built (one Sonnet session):**
- `src/hooks/use-coaching.ts` — `useLatestCoachingEvent` (dynamic 2s/10s refetchInterval, 404→null) + `useCoachingEvent(id)`
- `src/hooks/use-tv-token.ts` — `useTvToken` (404→null), `useRotateTvToken`, `useRevokeTvToken`
- Full coach panel component tree: BiasBadge, ConfidencePill, FreshnessPill, EventMeta, ChecklistRow, StrategyCard, InvalidStrategies, AlertsBanner, CoachingPanel
- Setup page components: TokenCard (generate/rotate/revoke with Dialog confirmations), PineScriptCard (download + collapsible preview + copy), TvSetupInstructions (numbered steps + security callout)
- Pages: `/coach` and `/coach/setup`, both under ProtectedLayout
- Sidebar: Sparkles icon "AI Coach" nav item
- Static asset: `public/neurospect-coach.pine` (mirror of wiki canonical)

**Verified manually:** token generation, rotation, revocation, curl webhook test → panel cycles pending→complete with bias, narrative, strategy cards, checklist rows, freshness pill.

**Deviation:** Used `CoachBias` type (not `Bias`) to avoid collision with existing `BiasType` trade type. Constants use `COACH_BIAS_LABELS`/`COACH_BIAS_STYLES`.

**Bug fixed during verification:** Frontend landed on port 5174 (5173 was occupied by a background dev server). CORS_ORIGINS only allowed 5173, causing 400 on OPTIONS preflight. Fixed by adding 5174 to CORS_ORIGINS in `.env`. Also: Postgres Docker container (`neurospect-db`) was not running — needed `docker start neurospect-db` before the backend could connect.

**Next session:** Render deployment + Discord OAuth. Boot prompt written in `processes/distributed-workflow/active/deployment.md`.

## Note
type: action
ref: deployment

Next session: deploy to Render (backend) + Cloudflare Pages (frontend) + real Discord OAuth. Boot prompt ready in `processes/distributed-workflow/active/deployment.md`. Key prerequisite to sort before starting: create a Discord application in the Developer Portal and note the client ID and client secret.
